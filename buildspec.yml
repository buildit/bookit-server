# Build specification for building project under Amazon CodeBuild

version: 0.2
env:
  variables:
    USE_CLOUD: "true"
phases:
  install:
    commands:
      - apt-get update
      - apt-get -y install curl
      - curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -
      - sudo apt-get install -y nodejs
      - npm --version
      - node --version
      - docker --version
  pre_build:
    commands:
      - echo -n "$CODEBUILD_BUILD_ID" | sed "s/.*:\([[:xdigit:]]\{7\}\).*/\1/" > /tmp/build_id.out
      - printf "%s:%s" "$REPOSITORY_URI" "$(cat /tmp/build_id.out)" > /tmp/build_tag.out
      - printf '{"tag":"%s"}' "$(cat /tmp/build_id.out)" > /tmp/build.json
      - cat /tmp/build_id.out
      - cat /tmp/build_tag.out
      - cat /tmp/build.json
      - npm install
      - $(aws ecr get-login)
  build:
    commands:
      - export CLOUD_CONFIG=`aws ssm get-parameter --name CLOUD_CONFIG --with-decryption --output text | cut -f 4`
      - export TEST_SECRET=`aws ssm get-parameter --name TEST_SECRET --with-decryption --output text | cut -f 4`
      - npm test
      - npm prune --production
      - docker build --tag "$(cat /tmp/build_tag.out)" .
  post_build:
    commands:
      - docker push "$(cat /tmp/build_tag.out)"
artifacts:
  files: /tmp/build.json
  discard-paths: yes
